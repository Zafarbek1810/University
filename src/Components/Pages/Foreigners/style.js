import { styled } from "styled-components";

const ForeignersWrapper = styled.div`
  .header {
    .navs {
      p {
        color: #000 !important;
      }
    }
  }
  .main {
    padding-top: 100px;
    padding-bottom: 72px;
  }
`;

export{ForeignersWrapper}